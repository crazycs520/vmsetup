global:
  user: "root"
  deploy_dir: "/deploy"
  data_dir: "/data"

server_configs:
  tidb:
    log.level: "error"
  tikv:
    log.level: "error"

pd_servers:
  - host: 127.0.0.1

tidb_servers:
  - host: 127.0.0.1

tikv_servers:
  - host: 127.0.0.1

tiflash_servers:
  - host: 127.0.0.1

monitoring_servers:
  - host: 127.0.0.1
    ng_port: 12020

grafana_servers:
  - host: 127.0.0.1

alertmanager_servers:
  - host: 127.0.0.1
